<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.foxconn.test">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.TestListViewLoadMoreActivity"
            android:launchMode="singleTask"/>
        <activity android:name=".activity.TestListViewLoadMoreActivity2"
            android:launchMode="singleTask"/>
        <activity android:name=".activity.TestListViewLoadMoreActivity3"
            android:launchMode="singleTask"/>
        <activity android:name=".test.TestActivity"
            android:launchMode="singleTask"/>
        <activity android:name=".ksoap2.MainKsoap2Activity"
            android:launchMode="singleTask"/>
        <activity android:name=".ShowActivity"
            android:launchMode="singleTask"/>
        <activity android:name=".activity.CallWebServiceActivity"
            android:launchMode="singleTask"/>
        <activity android:name=".webservice.WsActivity"
            android:launchMode="singleTask"/>
        <activity android:name=".socket.SocketActivity"
            android:launchMode="singleTask"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>
    </application>

</manifest>